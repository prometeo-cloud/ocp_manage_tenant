apiVersion: v1
kind: ResourceQuota
metadata:
  name: "{{ namespace }}_quota"
  namespace: "{{ project_name }}"
  selfLink: "/api/v1/namespaces/{{ namespace }}/resourcequotas/{{ namespace }}_quota"
spec:
  hard:
    cpu: "{{ item.cpu | default(2) }}"
    memory: "{{ item.memory | default(1) }}G"